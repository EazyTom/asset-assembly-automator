.venv/
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.log
.env
secrets.env
.asset_assembly_automator/secrets.env
*.key
meshy-api.key
magnific-api.key
*.db
*.db-journal
*.db-wal
*.db-shm
AssetAssemblyAutomator/
%LOCALAPPDATA%/
%USERPROFILE%/
meshy_output/

# Local sample / test artifacts (do not publish)
_meshy_extract/
Meshy_AI_*/
FloodCircuit_*.png
/*.zip

# Secrets / credentials (belt-and-suspenders — never commit)
*.pem
*.pfx
*.p12
credentials.json
# Higgsfield MCP OAuth tokens (normally under %LOCALAPPDATA%, but guard anyway)
higgsfield_oauth.json
mcp/higgsfield_oauth.json

# Cursor / MCP local config (often contains API keys)
.mcp.json
mcp.json
.cursor/mcp.json
!mcp.json.example

# IDE / OS
.DS_Store
Thumbs.db
desktop.ini

# Cursor local artifacts (plans may contain paths; keep hooks if desired)
.cursor/plans/
