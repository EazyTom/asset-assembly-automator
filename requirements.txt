# Runtime dependencies for Asset Assembly Automator.
# Preferred install (editable + scripts): pip install -e .
# Alternative: pip install -r requirements.txt

PyQt6>=6.6
qasync>=0.27
httpx>=0.27
pydantic>=2.6
pydantic-settings>=2.2
pyyaml>=6.0
structlog>=24.1
tenacity>=8.2
keyring>=25.0
watchdog>=4.0
Pillow>=10.2
markdown>=3.5
pyqtdarktheme-fork>=2.3
mcp>=1.28
